/// @brief This header file includes all the other calculator headers.
///
/// # Examples
/// ```
/// using namespace calc;
/// assert_eq(add(1, 2), 3);
/// assert_eq(subtract(2, 1), 1);
/// assert_eq(multiply(2, 3), 6);
/// ```
#pragma once

#include "add.h"
#include "subtract.h"
#include "multiply.h"
#include "divide.h"
